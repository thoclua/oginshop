@model IEnumerable<Category>
@if (Model != null && Model.Any())
{

    @foreach (var item in Model)
    {
       
          
                
                       
                            <ul>
                                <li class="active" data-filter="*">All</li>
                                <li data-filter=".@item.id.ToString()">@item.Name</li>
                                
                            </ul>
                        
                 

                
       
       
             

    }
}