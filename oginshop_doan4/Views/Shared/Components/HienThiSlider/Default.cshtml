@model IEnumerable<slide>
@if (Model != null && Model.Any())
{

	@foreach (var item in Model)
	{
                 
                        
                            <div class="banner__pic">
                                <img src="@item.slide_value" alt="">
                            </div>
                       
                       @* <div class="col-lg-6 col-md-6 col-sm-6">
                            <div class="banner__pic">
                                <img src="img/banner/banner-2.jpg" alt="">
                            </div>
                        </div>*@
                    
	}

}
